# MVC + REST MongoDB API with FastAPI

This is a simple FastAPI application that follows the MVC (Model-View-Controller) pattern and uses MongoDB for data storage.

## Requirements

- Python 3.x
- FastAPI
- MongoDB
